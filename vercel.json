{
  "buildCommand": "cd PersonalFit && npm run build",
  "outputDirectory": "PersonalFit/dist",
  "installCommand": "npm install && cd PersonalFit && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
